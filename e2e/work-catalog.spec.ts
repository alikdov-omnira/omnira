import {expect,test} from "@playwright/test";
async function login(page:any,email="admin@demo.odls"){await page.goto("/#login");await page.getByLabel("Tenant").fill("demo");await page.getByLabel("Email").fill(email);await page.getByLabel("Password").fill("DemoPassword!2026");await page.getByRole("button",{name:"Sign in"}).click();await expect(page.getByRole("heading",{name:"Executive Dashboard"})).toBeVisible();}
test("authorized user manages an international construction work definition",async({page})=>{const stamp=Date.now(),categoryCode=`E2E_CAT_${stamp}`,itemCode=`E2E_WORK_${stamp}`;await login(page);await page.getByRole("button",{name:"Construction Work Catalog"}).click();await expect(page.getByRole("heading",{name:"Construction Work Catalog"})).toBeVisible();await page.getByRole("button",{name:"Expand Finishing"}).click();await expect(page.getByRole("button",{name:"PAINTING — Painting"})).toBeVisible();await page.getByLabel("Category code").fill(categoryCode);await page.getByLabel("Category name").fill("E2E custom category");await page.getByLabel("Parent category").selectOption({label:"FINISHING — Finishing"});await page.getByRole("button",{name:"Create category"}).click();await expect(page.getByRole("button",{name:`${categoryCode} — E2E custom category`})).toBeVisible();await page.getByLabel("Work item code").fill(itemCode);await page.getByLabel("Work item name").fill("E2E custom work");await page.getByLabel("Create work item category").selectOption({label:`${categoryCode} — E2E custom category`});await page.getByLabel("Create work item measurement unit").selectOption({label:"M2 — Square meter"});await page.getByRole("button",{name:"Create work item"}).click();await expect(page.getByRole("table").getByText("E2E custom work",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Edit work item"}).click();await page.getByLabel("Edit work item name").fill("E2E edited work");await page.getByRole("button",{name:"Save work item"}).click();await expect(page.getByRole("table").getByText("E2E edited work",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Deactivate work item"}).click();await expect(page.getByRole("table").getByText("inactive",{exact:true})).toBeVisible();await page.getByRole("button",{name:`${categoryCode} — E2E custom category`}).click();await page.getByLabel("Edit category name").fill("E2E edited category");await page.getByRole("button",{name:"Save category"}).click();await expect(page.getByRole("button",{name:`${categoryCode} — E2E edited category`})).toBeVisible();});
test("operational employee reads but cannot mutate the work catalog",async({page})=>{await login(page,"employee@demo.odls");await page.getByRole("button",{name:"Construction Work Catalog"}).click();await expect(page.getByLabel("Work category tree")).toBeVisible();await expect(page.getByRole("table")).toBeVisible();await expect(page.getByRole("heading",{name:"Create category"})).toHaveCount(0);await expect(page.getByRole("button",{name:"Edit work item"})).toHaveCount(0);await expect(page.getByRole("button",{name:/Deactivate (category|work item)/})).toHaveCount(0);});
