import{expect,test}from"@playwright/test";
async function openScanner(page:any){await page.goto("/");await page.getByLabel("Tenant").fill("demo");await page.getByLabel("Email").fill("admin@demo.odls");await page.getByLabel("Password").fill("DemoPassword!2026");await page.getByRole("button",{name:"Sign in"}).click();await expect(page.getByRole("heading",{name:"Executive Dashboard"})).toBeVisible();await page.getByRole("button",{name:"Projects",exact:true}).click();await page.getByLabel("Search projects").fill("Demo Project");await page.getByRole("button",{name:"Open workspace"}).click();await page.getByRole("button",{name:"Room Scanner",exact:true}).click()}
test("keeps manual scanner intact and reports spatial support truthfully",async({page})=>{await openScanner(page);await expect(page.getByRole("button",{name:/^Measurements \d+$/,exact:true})).toBeVisible();await page.getByRole("button",{name:"Spatial capture",exact:true}).click();await expect(page.getByRole("heading",{name:"Spatial room capture"})).toBeVisible();await expect(page.getByText(/No camera-derived distance is claimed/)).toBeVisible();await expect(page.getByText(/calibrated_camera:/)).toBeVisible();await expect(page.getByRole("button",{name:"Start manual spatial session"})).toBeVisible();await page.getByRole("button",{name:"Room passport",exact:true}).click();await expect(page.getByRole("heading",{name:"Approved room passport"})).toBeVisible();await expect(page.getByText("Passport unavailable")).toBeVisible()});
