import {expect,test} from "@playwright/test";
async function login(page:any,email="admin@demo.odls"){await page.goto("/");await page.getByLabel("Tenant").fill("demo");await page.getByLabel("Email").fill(email);await page.getByLabel("Password").fill("DemoPassword!2026");await page.getByRole("button",{name:"Sign in"}).click();await expect(page.getByRole("heading",{name:"Executive Dashboard"})).toBeVisible();}
test("authorized user manages a material definition",async({page})=>{const stamp=Date.now(),categoryCode=`E2E_MCAT_${stamp}`,materialCode=`E2E_MAT_${stamp}`;await login(page);await page.getByRole("button",{name:"Material Catalog"}).click();await expect(page.getByRole("heading",{name:"Material Catalog"})).toBeVisible();await expect(page.getByLabel("Material category tree")).toContainText("BINDERS");await page.getByLabel("Material category code").fill(categoryCode);await page.getByLabel("Material category name").fill("E2E material category");await page.getByRole("button",{name:"Create material category"}).click();await expect(page.getByRole("button",{name:`${categoryCode} — E2E material category`})).toBeVisible();await page.getByLabel("Material code").fill(materialCode);await page.getByLabel("Material name").fill("E2E material");await page.getByLabel("Create material category").selectOption({label:`${categoryCode} — E2E material category`});await page.getByLabel("Create material measurement unit").selectOption({label:"KG — Kilogram"});await page.getByLabel("Technical data JSON").fill('{"grade":"A"}');await page.getByRole("button",{name:"Create material",exact:true}).click();await expect(page.getByRole("table").getByText("E2E material",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Edit material"}).click();await page.getByLabel("Edit material name").fill("E2E edited material");await page.getByRole("button",{name:"Save material"}).click();await expect(page.getByRole("table").getByText("E2E edited material",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Deactivate material"}).click();await expect(page.getByRole("table").getByText("inactive",{exact:true})).toBeVisible();});
test("employee reads but cannot mutate the material catalog",async({page})=>{await login(page,"employee@demo.odls");await page.getByRole("button",{name:"Material Catalog"}).click();await expect(page.getByLabel("Material category tree")).toBeVisible();await expect(page.getByRole("table")).toBeVisible();await expect(page.getByRole("heading",{name:"Create material"})).toHaveCount(0);await expect(page.getByRole("button",{name:"Edit material"})).toHaveCount(0);});
