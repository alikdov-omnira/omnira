import{describe,expect,it}from"vitest";import type{SpatialMeasurementProviderPort}from"../src/application/room-scanner/spatial-measurement-provider.js";import{normalizeSpatialProposal}from"../src/domain/room-scanner/spatial-geometry.js";
class SafeAdapter implements SpatialMeasurementProviderPort{providerType="test_adapter";async capabilities(){return{supported:true}}async measure(){return{providerType:"test_adapter"as const,coordinateSystem:"fixture-room",start:{x:0,y:0,z:0},end:{x:2,y:0,z:0},uncertaintyM:.01,confidence:1,trackingQuality:"stable"as const,calibrationState:"complete"as const,capturedAt:"2026-08-06T10:00:00.000Z"}}}
describe("spatial provider port",()=>{it("normalizes a safe adapter result without leaking a vendor payload",async()=>{const p=normalizeSpatialProposal(await new SafeAdapter().measure());expect(p.measuredDistanceM).toBe(2);expect(p).not.toHaveProperty("rawPayload")});it("keeps camera approximation proposed and blocking",()=>{const p=normalizeSpatialProposal({providerType:"web_xr",coordinateSystem:"x",start:{x:0,y:0,z:0},end:{x:1,y:0,z:0},uncertaintyM:.2,confidence:.5,trackingQuality:"limited",calibrationState:"complete",capturedAt:"2026-08-06T10:00:00.000Z"});expect(p).toMatchObject({accuracyClass:"approximate",verificationState:"proposed",blocksApproval:true})})});
