import {Pool} from "pg";import {afterAll,beforeAll,describe,expect,it} from "vitest";
const url=process.env.DATABASE_URL,run=url?describe:describe.skip;
run("estimate engine forced RLS",()=>{const owner=new Pool({connectionString:url}),roleUrl=new URL(url!);roleUrl.username="estimate_engine_rls_test";roleUrl.password="estimate_engine_rls_test";const restricted=new Pool({connectionString:roleUrl.toString()}),demo="00000000-0000-4000-8000-000000000001",other="00000000-0000-4000-8000-000000000002";
 beforeAll(async()=>{await owner.query("DO $$ BEGIN IF NOT EXISTS(SELECT 1 FROM pg_roles WHERE rolname='estimate_engine_rls_test') THEN CREATE ROLE estimate_engine_rls_test LOGIN PASSWORD 'estimate_engine_rls_test'; END IF; END $$");await owner.query("ALTER ROLE estimate_engine_rls_test PASSWORD 'estimate_engine_rls_test'");await owner.query("GRANT USAGE ON SCHEMA public TO estimate_engine_rls_test");await owner.query("GRANT SELECT,INSERT,UPDATE,DELETE ON estimates,estimate_items,estimate_materials TO estimate_engine_rls_test");});afterAll(async()=>{await owner.end();await restricted.end();});
 const tx=async(t:string,fn:(c:any)=>Promise<any>)=>{const c=await restricted.connect();try{await c.query("BEGIN");await c.query("SELECT set_config('app.tenant_id',$1,true)",[t]);const x=await fn(c);await c.query("ROLLBACK");return x;}finally{c.release();}};
 it("shows only current-tenant rows",async()=>{expect(Number((await tx(demo,c=>c.query("SELECT count(*) count FROM estimates"))).rows[0].count)).toBeGreaterThanOrEqual(3);expect(Number((await tx(other,c=>c.query("SELECT count(*) count FROM estimate_materials WHERE tenant_id=$1",[demo]))).rows[0].count)).toBe(0);});
 it("blocks cross-tenant writes",async()=>{await expect(tx(demo,c=>c.query("INSERT INTO estimates(tenant_id,project_id,code,display_name,currency,created_by,updated_by) VALUES($1,'00000000-0000-4000-8000-000000000071','RLS_EST','X','EUR','00000000-0000-4000-8000-000000000015','00000000-0000-4000-8000-000000000015')",[other]))).rejects.toMatchObject({code:"42501"});});
});
