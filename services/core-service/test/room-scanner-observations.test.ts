import{describe,expect,it}from"vitest";
import{validateCorner,validateDefect,validateFlatness}from"../src/domain/room-scanner/observation-rules.js";
describe("typed room scanner observations",()=>{
 it("derives flatness extrema and deterministic sample order",()=>{expect(validateFlatness({spanM:2,spanUnit:"m",samples:[{positionM:2,deviationM:-.003},{positionM:0,deviationM:.002},{positionM:1,deviationM:.001}]})).toEqual({samples:[{positionM:0,deviationM:.002},{positionM:1,deviationM:.001},{positionM:2,deviationM:-.003}],maxPositiveDeviationM:.002,maxNegativeDeviationM:-.003,totalRangeM:.005});});
 it("rejects invalid flatness numbers, positions, units and duplicates",()=>{expect(()=>validateFlatness({spanM:Number.NaN,spanUnit:"m",samples:[{positionM:0,deviationM:0}]})).toThrow();expect(()=>validateFlatness({spanM:1,spanUnit:"m",samples:[{positionM:2,deviationM:0}]})).toThrow();expect(()=>validateFlatness({spanM:1,spanUnit:"m",samples:[{positionM:0,deviationM:0},{positionM:0,deviationM:.1}]})).toThrow();expect(()=>validateFlatness({spanM:1,spanUnit:"cm"as"m",samples:[{positionM:0,deviationM:0}]})).toThrow();});
 it("requires typed defect classification, severity and dimensions",()=>{expect(validateDefect({defectType:"crack",severity:"major",positionXM:1,positionYM:2,lengthM:.4})).toMatchObject({defectType:"crack",severity:"major"});expect(()=>validateDefect({defectType:"magic",severity:"major",positionXM:0,positionYM:0,lengthM:1})).toThrow();expect(()=>validateDefect({defectType:"crack",severity:"unknown",positionXM:0,positionYM:0,lengthM:1})).toThrow();expect(()=>validateDefect({defectType:"crack",severity:"minor",positionXM:0,positionYM:0})).toThrow();expect(()=>validateDefect({defectType:"crack",severity:"minor",positionXM:0,positionYM:0,lengthM:-1})).toThrow();});
 it("validates physical corner angles and derives signed deviation",()=>{expect(validateCorner({positionM:1,expectedAngleDeg:90,measuredAngleDeg:88.5}).deviationDeg).toBe(-1.5);expect(()=>validateCorner({positionM:0,expectedAngleDeg:360,measuredAngleDeg:90})).toThrow();expect(()=>validateCorner({positionM:0,expectedAngleDeg:90,measuredAngleDeg:Number.POSITIVE_INFINITY})).toThrow();});
});
