import{describe,it,expect}from"vitest";import{AgentRegistry,agentRegistry}from"../src/domain/ai-platform/agent-registry.js";import{documentQuery,missingPermissions,routeCommand}from"../src/domain/ai-platform/orchestrator-rules.js";
describe("OMNIRO Agent Registry and routing",()=>{it("registers the one shared specialist set",()=>{expect(agentRegistry.definitions.map(x=>x.id)).toEqual(["project-analyst","document","finance","legal","engineering","communication","support","calendar-notes"]);expect(()=>new AgentRegistry([agentRegistry.definitions[0],agentRegistry.definitions[0]])).toThrow(/Duplicate OMNIRO agent/)});it.each([["Напиши договор с клиентом Ковальски","legal","draft_contract"],["Покажи фактуры за август","finance","list_invoices"],["Почему выросла смета?","project-analyst","explain_estimate"],["Покажи погодные риски","project-analyst","weather_risk"],["Напиши клиенту, что работы переносятся","communication","draft_message"],["Добавь заметку","calendar-notes","calendar_notes"]])("routes %s deterministically",(text,agent,intent)=>expect(routeCommand(text)).toMatchObject({agentId:agent,intent}));it("normalizes document intent while preserving contextual search",()=>expect(documentQuery("Покажи последний утверждённый чертёж ванной")).toEqual({approved:true,type:"drawing",search:"ванной"}));it("never expands permission context",()=>expect(missingPermissions(["documents.read","projects.read"],["projects.read"])).toEqual(["documents.read"]))});
