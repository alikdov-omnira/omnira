import{describe,expect,it}from"vitest";import{assertNoAudio,assertTransition,assertVersion}from"../src/domain/communication/communication-rules.js";import{defaultCommunicationProviders}from"../src/application/communication/communication-provider.js";
describe("Communication Hub domain",()=>{it("enforces the human-reviewed lifecycle",()=>{expect(()=>assertTransition("DRAFT","SENT")).toThrow();expect(()=>assertTransition("DRAFT","READY_FOR_REVIEW")).not.toThrow();expect(()=>assertTransition("READY_FOR_REVIEW","APPROVED_TO_SEND")).not.toThrow();expect(()=>assertTransition("APPROVED_TO_SEND","SENT")).not.toThrow()});it("uses optimistic versions and never stores voice audio",()=>{expect(()=>assertVersion(2,1)).toThrow();expect(()=>assertNoAudio({audio:"bytes"})).toThrow();expect(()=>assertNoAudio({body:"text command"})).not.toThrow()});it("reports external providers unavailable without fake sending",async()=>{for(const provider of defaultCommunicationProviders.status())expect(provider.truth).toBe("UNAVAILABLE");await expect(defaultCommunicationProviders.get("email")!.send({}as any)).rejects.toThrow("PROVIDER_UNAVAILABLE")})});
