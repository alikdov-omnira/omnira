import{describe,expect,it}from"vitest";import{evaluateDemands,validateNormReadiness}from"../src/domain/engineering-norm/engineering-norm-rules.js";
const parameters=[{code:"thickness_mm",dataType:"decimal"as const,minimumValue:8,maximumValue:20,allowedValues:[],required:true,defaultValue:null}];const demands=[{demandKind:"material_requirement"as const,requirementCode:"GYPSUM_DRY_MIX",outputUnitCode:"kg",formula:{kind:"linear"as const,base:0,perWorkUnit:true,terms:[{parameter:"thickness_mm",coefficient:0.85}]}}];
describe("engineering norm rules",()=>{it("evaluates coefficients stored in the approved norm",()=>expect(evaluateDemands(42.6,parameters,demands,{thickness_mm:12})).toEqual([{demandKind:"material_requirement",requirementCode:"GYPSUM_DRY_MIX",quantity:434.52,unitCode:"kg"}])) ;it("enforces approved parameter ranges",()=>expect(()=>evaluateDemands(42.6,parameters,demands,{thickness_mm:25})).toThrow("ENGINEERING_NORM_PARAMETER_OUT_OF_RANGE"));it("requires all authoritative demand dimensions",()=>expect(validateNormReadiness(parameters,[{}],demands)).toEqual([{code:"ENGINEERING_NORM_DEMAND_REQUIRED",detail:"labor"},{code:"ENGINEERING_NORM_DEMAND_REQUIRED",detail:"machine"}]))});
