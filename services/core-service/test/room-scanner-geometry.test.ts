import{describe,expect,it}from"vitest";import{measurementCompleteness,openingGeometry,polygonArea,polygonPerimeter,rectangularRoom}from"../src/domain/room-scanner/geometry-engine.js";
describe("room scanner geometry",()=>{it("calculates polygon geometry",()=>{const p=[{x:0,y:0},{x:4,y:0},{x:4,y:3},{x:0,y:3}];expect(polygonArea(p)).toBe(12);expect(polygonPerimeter(p)).toBe(14);});it("calculates openings and reveals",()=>{expect(openingGeometry(1.5,1.4,.2)).toEqual({area:2.0999999999999996,perimeter:5.8,revealArea:1.16,cornerLength:4.3});});it("produces explainable deterministic room quantities and deductions",()=>{const input={roomId:"r",widthId:"w",lengthId:"l",heightId:"h",width:4,length:5,height:2.7,openings:[{id:"o-2",width:.5,height:1,depth:.1},{id:"o-1",width:1,height:2,depth:.2}]},a=rectangularRoom(input),again=rectangularRoom({...input,openings:[...input.openings].reverse()}),q=(t:string)=>a.find(x=>x.type===t)!;expect(a).toEqual(again);expect(q("floor_area").value).toBe(20);expect(q("wall_gross_area").value).toBe(48.6);expect(q("wall_net_area").value).toBe(46.1);expect(q("room_volume").value).toBe(54);expect(q("wall_net_area").inputReferences).toEqual(["w","l","h","o-1","o-2"]);for(const quantity of a){expect(quantity.formulaId).toBeTruthy();expect(quantity.calculationVersion).toBe("room-geometry-v1");expect(quantity.inputReferences).toBeInstanceOf(Array);}});it("reports structured completeness",()=>{const x=measurementCompleteness({scanId:"s",roomId:"r",widthId:"w"});expect(x.status).toBe("incomplete");expect(x.issues.map(i=>i.code)).toEqual(["ROOM_LENGTH_MISSING","ROOM_HEIGHT_MISSING"]);});it("rejects invalid, zero, non-finite and impossible geometry",()=>{expect(()=>polygonArea([{x:0,y:0},{x:1,y:1},{x:2,y:2}])).toThrow();expect(()=>polygonPerimeter([{x:0,y:0},{x:Number.POSITIVE_INFINITY,y:1},{x:2,y:2}])).toThrow();expect(()=>rectangularRoom({roomId:"r",widthId:"w",lengthId:"l",heightId:"h",width:0,length:1,height:1})).toThrow();expect(()=>rectangularRoom({roomId:"r",widthId:"w",lengthId:"l",heightId:"h",width:1,length:1,height:1,openings:[{id:"too-large",width:10,height:10}]})).toThrow();});});
