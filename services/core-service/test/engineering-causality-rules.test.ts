import{describe,expect,it}from"vitest";import{explanation,lineageConfidence,validateEngineeringLineage,type CausalityNode}from"../src/domain/engineering-causality/engineering-causality-rules.js";
const make=(sourceModule:string,sourceObject:string):CausalityNode=>({sourceModule,sourceObject,sourceObjectId:sourceObject,version:1,snapshotId:null,snapshotFingerprint:null,approval:{status:"recorded",approvedBy:null,approvedAt:null},formula:{kind:"source"},dependencies:[],confidence:.92,revision:1,timestamp:"2026-08-07T00:00:00.000Z",operator:"human"});
describe("Engineering Causality Chain",()=>{it("explains a value in authoritative causal order",()=>{const nodes=[make("original_evidence","Photo"),make("scanner_measurement","Wall area"),make("digital_room_passport","Room Passport"),make("work_scope","Painting scope"),make("technology","Two coats"),make("construction_knowledge","Finish rules"),make("engineering_norm","Norm V3"),make("material_consumption","Paint quantity"),make("regional_pricing","Warsaw rates"),make("commercial_estimate","Estimate line")];expect(validateEngineeringLineage(nodes)).toBe(nodes);expect(lineageConfidence(nodes)).toBe(.92);expect(explanation("gross_total",456,nodes)).toMatchObject({statement:expect.stringContaining("This value exists because Photo"),complete:true,brokenLinks:[]})});it("rejects missing, broken, reversed, or unapproved lineage",()=>{expect(()=>validateEngineeringLineage([])).toThrow();expect(()=>validateEngineeringLineage([make("commercial_estimate","Cost"),make("scanner_measurement","Area")])).toThrow();expect(()=>validateEngineeringLineage([{...make("engineering_norm","Norm"),approval:{status:"approved",approvedBy:null,approvedAt:null}}])).toThrow()})});
