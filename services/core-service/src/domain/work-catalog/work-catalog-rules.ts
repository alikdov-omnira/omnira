import {domainErrors} from "../errors.js";
const codePattern=/^[A-Z][A-Z0-9_]{0,59}$/;
export const normalizeCatalogCode=(value:string)=>value.trim().toUpperCase();
const common=(code:string,name:string)=>{const normalized=normalizeCatalogCode(code);if(!codePattern.test(normalized))throw domainErrors.validation("Code must be a stable uppercase identifier");if(!name.trim())throw domainErrors.validation("Display name is required");return normalized;};
export function validateCategoryCreate<T extends {code:string;displayName:string;sortOrder:number}>(x:T){const code=common(x.code,x.displayName);if(!Number.isInteger(x.sortOrder)||x.sortOrder<0||x.sortOrder>1_000_000)throw domainErrors.validation("sortOrder must be between 0 and 1000000");return {...x,code,displayName:x.displayName.trim()};}
export function validateCategoryUpdate<T extends {displayName?:string;sortOrder?:number}>(x:T){if(x.displayName!==undefined&&!x.displayName.trim())throw domainErrors.validation("Display name is required");if(x.sortOrder!==undefined&&(!Number.isInteger(x.sortOrder)||x.sortOrder<0||x.sortOrder>1_000_000))throw domainErrors.validation("sortOrder must be between 0 and 1000000");return {...x,displayName:x.displayName?.trim()};}
export function validateWorkItemCreate<T extends {code:string;displayName:string;quantityPrecision:number}>(x:T){const code=common(x.code,x.displayName);if(!Number.isInteger(x.quantityPrecision)||x.quantityPrecision<0||x.quantityPrecision>12)throw domainErrors.validation("quantityPrecision must be between 0 and 12");return {...x,code,displayName:x.displayName.trim()};}
export function validateWorkItemUpdate<T extends {displayName?:string;quantityPrecision?:number}>(x:T){if(x.displayName!==undefined&&!x.displayName.trim())throw domainErrors.validation("Display name is required");if(x.quantityPrecision!==undefined&&(!Number.isInteger(x.quantityPrecision)||x.quantityPrecision<0||x.quantityPrecision>12))throw domainErrors.validation("quantityPrecision must be between 0 and 12");return {...x,displayName:x.displayName?.trim()};}
