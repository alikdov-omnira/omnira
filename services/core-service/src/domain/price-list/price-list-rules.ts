import {domainErrors} from "../errors.js";
const codePattern=/^[A-Z][A-Z0-9_]{0,59}$/,datePattern=/^\d{4}-\d{2}-\d{2}$/;
const text=(v:string,n:string,max:number)=>{const x=v.trim();if(!x)throw domainErrors.validation(`${n} is required`);if(x.length>max)throw domainErrors.validation(`${n} must not exceed ${max} characters`);return x;};
const optional=(v:string|null|undefined,n:string,max:number)=>{if(v==null)return v;const x=v.trim();if(!x)return null;if(x.length>max)throw domainErrors.validation(`${n} must not exceed ${max} characters`);return x;};
const alpha=(v:string,n:string,length:number)=>{const x=v.trim().toUpperCase();if(!new RegExp(`^[A-Z]{${length}}$`).test(x))throw domainErrors.validation(`${n} must be a ${length}-letter code`);return x;};
const date=(v:string,n:string)=>{if(!datePattern.test(v)||Number.isNaN(Date.parse(`${v}T00:00:00Z`)))throw domainErrors.validation(`${n} must be an ISO date`);return v;};
const decimal=(v:string,n:string,max:number)=>{if(!/^\d+(\.\d{1,4})?$/.test(v)||Number(v)<0||Number(v)>max)throw domainErrors.validation(`${n} must be between 0 and ${max} with at most 4 decimal places`);return Number(v).toFixed(4);};
const validity=(from:string,to?:string|null)=>{const f=date(from,"validFrom"),t=to==null?to:date(to,"validTo");if(t&&t<f)throw domainErrors.validation("validTo must be on or after validFrom");return [f,t] as const;};
export const validatePriceListCreate=(x:any)=>{const [validFrom,validTo]=validity(x.validFrom,x.validTo);const code=x.code.trim().toUpperCase();if(!codePattern.test(code))throw domainErrors.validation("Code must be a stable uppercase identifier");return {...x,code,displayName:text(x.displayName,"Display name",300),description:optional(x.description,"Description",5000)??undefined,currency:alpha(x.currency,"currency",3),country:alpha(x.country,"country",2),region:optional(x.region,"Region",200)??undefined,validFrom,validTo};};
export const validatePriceListUpdate=(x:any,current:{validFrom:string;validTo:string|null})=>{const [validFrom,validTo]=validity(x.validFrom??current.validFrom,x.validTo===undefined?current.validTo:x.validTo);return {...x,displayName:x.displayName===undefined?undefined:text(x.displayName,"Display name",300),description:x.description===undefined?undefined:optional(x.description,"Description",5000),currency:x.currency===undefined?undefined:alpha(x.currency,"currency",3),country:x.country===undefined?undefined:alpha(x.country,"country",2),region:x.region===undefined?undefined:optional(x.region,"Region",200),validFrom:x.validFrom===undefined?undefined:validFrom,validTo:x.validTo===undefined?undefined:validTo};};
export const validatePriceListItemCreate=(x:any)=>{const [validFrom,validTo]=validity(x.validFrom,x.validTo);return {...x,unitPrice:decimal(x.unitPrice,"unitPrice",999999999999999),vatRate:decimal(x.vatRate,"vatRate",100),discountPercent:decimal(x.discountPercent,"discountPercent",100),currency:alpha(x.currency,"currency",3),validFrom,validTo};};
export const validatePriceListItemUpdate=(x:any,current:{validFrom:string;validTo:string|null})=>{const [validFrom,validTo]=validity(x.validFrom??current.validFrom,x.validTo===undefined?current.validTo:x.validTo);return {...x,unitPrice:x.unitPrice===undefined?undefined:decimal(x.unitPrice,"unitPrice",999999999999999),vatRate:x.vatRate===undefined?undefined:decimal(x.vatRate,"vatRate",100),discountPercent:x.discountPercent===undefined?undefined:decimal(x.discountPercent,"discountPercent",100),currency:x.currency===undefined?undefined:alpha(x.currency,"currency",3),validFrom:x.validFrom===undefined?undefined:validFrom,validTo:x.validTo===undefined?undefined:validTo};};
