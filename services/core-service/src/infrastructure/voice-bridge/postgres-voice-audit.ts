import{randomUUID}from"node:crypto";import type{Pool}from"pg";import type{VoiceAuditPort}from"../../application/voice-bridge/voice-bridge-service.js";import type{VoiceActor}from"../../domain/voice-bridge/voice-bridge-types.js";import{inUserTransaction}from"../transaction.js";
export class PostgresVoiceAudit implements VoiceAuditPort{constructor(private pool:Pool){}record(a:VoiceActor,action:string,sessionId:string,metadata:Record<string,unknown>){return inUserTransaction(this.pool,a.tenantId,a.id,async db=>{await db.query("INSERT INTO audit_logs(tenant_id,actor_id,action,entity_type,entity_id,correlation_id,metadata)VALUES($1,$2,$3,'voice_session',$4,$5,$6)",[a.tenantId,a.id,action,sessionId,/^[0-9a-f-]{36}$/i.test(a.correlationId)?a.correlationId:randomUUID(),metadata]);})}}
