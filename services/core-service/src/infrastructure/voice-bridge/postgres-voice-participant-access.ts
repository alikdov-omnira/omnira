import type{Pool}from"pg";import type{VoiceParticipantAccessPort}from"../../application/voice-bridge/voice-bridge-service.js";import type{VoiceActor}from"../../domain/voice-bridge/voice-bridge-types.js";import{inUserTransaction}from"../transaction.js";
export class PostgresVoiceParticipantAccess implements VoiceParticipantAccessPort{constructor(private pool:Pool){}allBelongToTenant(a:VoiceActor,userIds:readonly string[]){const unique=[...new Set(userIds)];return inUserTransaction(this.pool,a.tenantId,a.id,async db=>{const result=await db.query<{count:string}>("SELECT count(*)::text count FROM users WHERE tenant_id=$1 AND id=ANY($2::uuid[]) AND deleted_at IS NULL AND is_disabled=false",[a.tenantId,unique]);return Number(result.rows[0]?.count??0)===unique.length})}}
