# OMNIRO Stage 2A — Spatial Refinement Review

DESIGN PROTOTYPE. SAMPLE PROJECT. NO LIVE TENANT DATA. No runtime integration is represented.

## Governing change

Operational information now emerges from the living OMNIRO topology. Rails, evidence, authority controls and explanations remain readable, but their geometry and connecting traces identify the system object they belong to. The project nucleus is no longer a diagram behind independent panels.

## Spatial system and project nucleus

The Core increased in operational scale and gained nested computational shells, project identity, lifecycle, region/currency and authoritative fingerprint rings. Project context is repeated progressively: the outer ring supplies identity, the lower authority ring supplies lifecycle and fingerprint, while the left context remains a secondary readable projection of the same nucleus.

Module identity is encoded spatially. Stable complete rings denote REAL capability; dashed or incomplete rings denote PARTIAL state; dormant dotted structures recede for UNAVAILABLE capability. Scanner uses evidence-grid geometry, Passport uses identity/gate geometry, norms use verification layers, Estimate uses convergence geometry, and Analytics uses signal geometry.

## Panels converted into relationships

The left and right areas have reduced opaque mass and use structural traces extending into the nucleus. Attention information unfolds from an amber authority line. Orchestrator is a system explanation band rather than an input form: invocation first changes the Scanner → gate → Passport path, then reveals the textual trace.

## Scanner spatial workspace

Scanner expands into the primary evidence agent instead of opening a centered application window. Its evidence grid, lifecycle, approved output, supporting documents and next Passport dependency occupy connected depth layers. The Core remains visible as operational presence and the original topology recedes rather than disappearing.

## Spatial causality

The estimate explanation is a reverse trace through spatial objects:

`Commercial Estimate ← Quantity ← Material ← Engineering Norm ← Source Evidence`

Each object has its own depth and source fragment. Thin reverse impulses and illumination establish direction. The textual heading classifies the concept as unavailable integration with no live value; it does not claim a persisted runtime trace.

## Human authority boundary

The payload remains physically suspended at an amber gate. The Passport agent enlarges, its incomplete authority ring activates, the downstream state stays waiting, and the attention projection points directly back to that gate. Human approval therefore reads as an interruption in the system, not a detached alert.

## Orchestrator topology reference

For “What is waiting for me?”, Scanner and Passport react first, the selected path changes to violet, and only then does the explanation layer unfold. The explanation names waiting entity, source, reason, blocking destination, permitted human action and next state while preserving REAL/DERIVED distinctions.

## Mobile

Mobile retains a compact project/Core nucleus, one visible flow and a physical authority gate. Approval and Orchestrator use full-height spatial sheets anchored to the Core. They are not reduced desktop rails. Touch targets remain at least approximately 44 px in primary actions and agents.

## Motion semantics

The disposable prototype defines visual states rather than a production animation system. Intended motion is restrained and semantic: calm breathing at rest, directional payload on flow, suspended tension at approval, topology response before Orchestrator text, and sequential reverse illumination for causality.

## Accessibility and performance

- Status never relies on color alone: ring completeness, line style, labels and gate symbols reinforce meaning.
- Primary operational text maintains high contrast; micro-labels are supplementary.
- Reduced-motion implementations should preserve state changes without travel animation.
- Production implementation should tier particles, shell depth and blur by device capability and DPR.
- Decorative atmosphere must remain excluded from the accessibility tree; nodes and authority actions require semantic labels and keyboard focus.

## Review-question audit

1. Without text, nucleus, agents, evidence geometry and authority gate retain system meaning.
2. Scanner evidence geometry is visually distinct from Estimate convergence geometry.
3. Payload and selected paths provide a visible direction of information.
4. Approval stops a visible payload at one exact authority point.
5. Orchestrator changes and highlights the topology it explains.
6. Causality is a reverse spatial trace, not five cards in a modal.
7. Scanner focus preserves both the Core and receded topology.
8. Project identity and authoritative context surround the Core.
9. Nested geometry, depth and meaningful density preserve Stage 1 visual DNA.
10. The composition reads as one spatial instrument rather than a sidebar-and-widget dashboard.
