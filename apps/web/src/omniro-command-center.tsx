import{useEffect,useMemo,useReducer,useState}from"react";
import type{Session}from"./api.js";
import{ErrorState,LoadingState,type WorkspaceTab}from"./construction-ui.js";
import{buildingModuleRegistry}from"./omniro-building-modules.js";
import{buildingFlowRegistry}from"./omniro-building-flows.js";
import{loadOmniroData,type OmniroLoadedData}from"./omniro-command-center-data.js";
import{parentOmniroRoute,parseOmniroRoute,serializeOmniroRoute,type OmniroRoute}from"./omniro-command-center-route.js";
import{initialInteraction,reduceInteraction}from"./omniro-interaction-model.js";
import{resolveRuntime}from"./omniro-building-adapters.js";
import{OmniroCommandCenterShell}from"./omniro-command-center-shell.js";
import type{OmniroModuleRuntimeAdapter,OmniroRuntimeInput}from"./omniro-module-contract.js";
import type{OmniroQuality}from"./omniro-operational-scene.js";

type Navigate=(page:"omniro"|"overview"|"construction-projects"|"project-workspace"|"documents"|"tasks",projectId?:string,tab?:WorkspaceTab)=>void;
const defaultQuality=():OmniroQuality=>typeof matchMedia!=="undefined"&&matchMedia("(prefers-reduced-motion: reduce)").matches?"reduced":typeof navigator!=="undefined"&&((navigator.hardwareConcurrency??8)<=4||navigator.maxTouchPoints>0)?"balanced":"enhanced";
export function OmniroCommandCenter({session,navigate,forceRendererFailure=false}:{session:Session;navigate:Navigate;forceRendererFailure?:boolean}){const[data,setData]=useState<OmniroLoadedData>(),[error,setError]=useState<unknown>(),[quality,setQuality]=useState<OmniroQuality>(defaultQuality),[interaction,dispatch]=useReducer(reduceInteraction,parseOmniroRoute(typeof location==="undefined"?"#omniro":location.hash),initialInteraction);const load=()=>{setError(undefined);void loadOmniroData(session).then(setData).catch(setError)};useEffect(load,[session]);useEffect(()=>{const restore=()=>dispatch({type:"ROUTE",route:parseOmniroRoute(location.hash)});addEventListener("hashchange",restore);addEventListener("popstate",restore);return()=>{removeEventListener("hashchange",restore);removeEventListener("popstate",restore)}},[]);const project=data?.projects.find(x=>x.id===interaction.route.projectId)??data?.projects[0],sources=project&&data?.byProject.get(project.id),adapters=useMemo(()=>new Map(buildingModuleRegistry.definitions.map(x=>[x.id,x.runtime as OmniroModuleRuntimeAdapter])),[]),runtime=useMemo(()=>project&&sources?resolveRuntime(project.id,session,sources,adapters):undefined,[adapters,project,session,sources]),flows=useMemo(()=>{if(!runtime||!sources)return[];const input:OmniroRuntimeInput={projectId:runtime.projectId,session,sources:sources as unknown as Record<string,unknown>};return buildingFlowRegistry.definitions.map(x=>x.resolve(input,runtime))},[runtime,session,sources]);const selectedId=interaction.route.moduleId??"project",selected=runtime?.modules.get(selectedId),definition=buildingModuleRegistry.get(selectedId),explanation=selected&&runtime&&definition?definition.explain.explain(selected,runtime):undefined;const go=(route:OmniroRoute,replace=false)=>{const hash=serializeOmniroRoute(route);history[replace?"replaceState":"pushState"]({omniro:true},"",`${location.pathname}${location.search}${hash}`);dispatch({type:"ROUTE",route})};if(error)return <section className="oc-load"><ErrorState error={error} onRetry={load}/></section>;if(!data)return <section className="oc-load"><LoadingState label="Resolving authenticated OMNIRO project context"/></section>;if(!project||!sources||!runtime)return <section className="oc-load"><p>No authoritative projects are available.</p></section>;return <OmniroCommandCenterShell session={session} projects={data.projects} project={project} runtime={runtime} flows={flows} route={interaction.route} quality={quality} forceFallback={forceRendererFailure} explanation={explanation} explanationOpen={interaction.explanationOpen} onProject={id=>go({level:"focus",projectId:id,moduleId:"project"})} onFocus={id=>go({level:"focus",projectId:project.id,moduleId:id})} onWorkspace={id=>{const item=buildingModuleRegistry.get(id);if(item?.workspace&&runtime.modules.get(id)?.availability!=="unavailable")go({level:"workspace",projectId:project.id,moduleId:id,entityId:runtime.modules.get(id)?.record?.entityId})}} onCloseWorkspace={()=>go(parentOmniroRoute(interaction.route))} onToggleExplanation={()=>dispatch({type:interaction.explanationOpen?"CLOSE_EXPLANATION":"EXPLAIN"})} onQuality={setQuality} onNavigateContext={page=>navigate(page,project.id)}/>}
