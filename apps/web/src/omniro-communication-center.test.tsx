import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{OmniroCommunicationCenter}from"./omniro-communication-center.js";
describe("Communication Center",()=>{it("keeps human communication separate from notifications and labels the visual shell",()=>{const html=renderToStaticMarkup(<OmniroCommunicationCenter open session={{accessToken:"x",refreshToken:"x",user:{id:"u",email:"u@test",displayName:"User"},permissions:["communications.read"]}} projectId="00000000-0000-4000-8000-000000000001" onClose={()=>{}}/>);expect(html).toContain("separate from system Notifications");expect(html).toContain("FINAL VISUAL = OWNER APPROVAL REQUIRED");expect(html).toContain("Provider truth")})});
