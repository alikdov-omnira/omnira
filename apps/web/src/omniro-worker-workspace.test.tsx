import{describe,expect,it}from"vitest";import type{TaskContract}from"@odls/contracts";import{dueToday,isAssignedTo,workerAction,workerCapabilityStates}from"./omniro-worker-workspace.js";import{roleWorkspaceRegistry}from"./omniro-role-workspace-registry.js";
const task={id:"00000000-0000-4000-8000-000000000001",tenantId:"00000000-0000-4000-8000-000000000002",projectId:"00000000-0000-4000-8000-000000000003",title:"Install",description:"Room: 204",status:"in_progress",priority:"normal",dueDate:"2026-08-20",startedAt:null,completedAt:null,isOverdue:false,assignees:[{userId:"00000000-0000-4000-8000-000000000004",displayName:"Worker",assignedAt:"2026-08-19T10:00:00Z"}],version:1,createdAt:"2026-08-19T10:00:00Z",updatedAt:"2026-08-19T10:00:00Z",archivedAt:null}satisfies TaskContract;
describe("Worker Workspace contracts",()=>{it("keeps assignment-scoped task projection",()=>{expect(isAssignedTo(task,task.assignees[0]!.userId)).toBe(true);expect(isAssignedTo(task,"00000000-0000-4000-8000-000000000099")).toBe(false);expect(dueToday(task,new Date("2026-08-20T08:00:00Z"))).toBe(true)});it("exposes only worker-owned lifecycle actions",()=>{expect(workerAction("todo")).toBe("start");expect(workerAction("returned")).toBe("start");expect(workerAction("in_progress")).toBe("submit");expect(workerAction("submitted_for_review")).toBeNull();expect(workerAction("accepted")).toBeNull()});it("registers the REAL permission-grounded role without overstating platform gaps",()=>{const session={accessToken:"",refreshToken:"",user:{id:task.assignees[0]!.userId,email:"worker@test",displayName:"Worker"},permissions:["tasks.read","tasks.self_update"]};expect(roleWorkspaceRegistry.resolve("worker",session).truth).toBe("REAL");expect(roleWorkspaceRegistry.resolve("worker",{...session,permissions:["tasks.read"]}).truth).toBe("UNAVAILABLE");expect(workerCapabilityStates.review).toBe("REAL");expect(workerCapabilityStates.materials).toBe("PARTIAL");expect(workerCapabilityStates.correspondence).toBe("UNAVAILABLE")})});
