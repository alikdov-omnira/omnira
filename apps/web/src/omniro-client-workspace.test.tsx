import{describe,expect,it}from"vitest";import{derivedProgress}from"./omniro-client-workspace.js";import{roleWorkspaceRegistry}from"./omniro-role-workspace-registry.js";
const session={accessToken:"",refreshToken:"",user:{id:"client-user",email:"client@example.test",displayName:"Client Actor"},permissions:["client_workspace.read","client_requests.create","client_approvals.decide","notifications.read"]};
describe("Client Workspace contracts",()=>{it("derives progress only from real task counts",()=>{expect(derivedProgress({taskCount:4,completedTaskCount:3})).toBe(75);expect(derivedProgress({taskCount:0,completedTaskCount:0})).toBeNull()});it("activates Client through the existing role registry and preserves actor identity",()=>{const resolved=roleWorkspaceRegistry.resolve("client",session);expect(resolved.truth).toBe("REAL");expect(resolved.actualActorId).toBe("client-user");expect(roleWorkspaceRegistry.resolve("client",{...session,permissions:[]}).truth).toBe("UNAVAILABLE")});it("keeps shared providers truthful",()=>{const role=roleWorkspaceRegistry.get("client")!;expect(role.assistantCapabilities.find(x=>x.id==="ai-secretary")?.truth).toBe("PARTIAL");expect(role.communicationCapabilities.filter(x=>["correspondence","voice","external"].includes(x.id)).every(x=>x.truth==="UNAVAILABLE")).toBe(true);expect(role.communicationCapabilities.find(x=>x.id==="voice")?.reason).toMatch(/never recorded or stored/)})});
