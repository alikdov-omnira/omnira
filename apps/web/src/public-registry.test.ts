import{describe,expect,it}from"vitest";import{capabilities,copy,demonstratedCapabilities,publicCapability}from"./public-registry.js";
describe("public capability registry",()=>{it("uses explicit truthful states and evidence for every demonstrated capability",()=>{expect(demonstratedCapabilities.length).toBeGreaterThan(5);expect(demonstratedCapabilities.every(item=>item.evidence.length>0)).toBe(true);expect(capabilities.find(item=>item.id==="weather")?.state).toBe("unavailable")});it("has stable lookup and six public locales",()=>{expect(publicCapability("estimate")?.state).toBe("available");expect(Object.keys(copy)).toEqual(["en","nl","de","pl","ru","uk"])});});
