import{lazy,Suspense,useMemo}from"react";import type{Session}from"./api.js";import{LoadingState}from"./construction-ui.js";import type{OmniroModuleDefinition,OmniroModuleRuntimeState}from"./omniro-module-contract.js";import type{OmniroWorkspaceRegistry}from"./omniro-workspace-registry.js";
export function OmniroFocusFrame({definition,runtime,session,projectId,registry,onClose}:{definition:OmniroModuleDefinition;runtime:OmniroModuleRuntimeState;session:Session;projectId:string;registry:OmniroWorkspaceRegistry;onClose:()=>void}){const workspace=definition.workspace?registry.get(definition.workspace.id):undefined,allowed=workspace?.requiredPermissions.every(x=>session.permissions.includes(x)),Content=useMemo(()=>workspace?lazy(workspace.load):undefined,[workspace]);return <section className="oc-focus-frame" aria-labelledby="oc-workspace-title"><header><button onClick={onClose}>← Return to system</button><div><small>AGENT WORKSPACE · SAME OPERATIONAL SYSTEM</small><h2 id="oc-workspace-title">{definition.label}</h2><span>{runtime.reason}</span></div><strong>{runtime.truth}</strong></header>{!workspace?<p role="status">This capability is not connected in Stage 2A.</p>:!allowed?<p role="alert">You do not have permission to enter this workspace.</p>:Content?<Suspense fallback={<LoadingState label={`Opening ${definition.label}`}/>}><Content session={session} projectId={projectId} runtime={runtime} onClose={onClose}/></Suspense>:null}</section>}
