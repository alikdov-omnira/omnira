import type{ProjectContract}from"@odls/contracts";import type{OmniroModuleRuntimeState}from"./omniro-module-contract.js";
export function OmniroProjectNucleus({project,scanner}:{project:ProjectContract;scanner?:OmniroModuleRuntimeState}){return <div className="oc-nucleus-label" aria-label={`${project.name}, ${project.projectNumber}, ${project.status}`}><small>{project.projectNumber} · {project.status.toUpperCase()}</small><strong>OMNIRO</strong><span>{project.name}</span><em>{project.currencyCode} · {scanner?.record?`SCAN V${scanner.record.version??"—"}`:"NO SCAN SNAPSHOT"}</em></div>}
