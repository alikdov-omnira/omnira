import type{ProjectContract}from"@odls/contracts";import{api,type DesignProjectDto,type RoomScanDto,type Session,type TechnicalAssignmentDto}from"./api.js";import{hasPermission}from"./construction-ui.js";import type{BuildingSources}from"./omniro-building-adapters.js";
export type OmniroLoadedData={projects:ProjectContract[];byProject:Map<string,BuildingSources>;partial:string[]};
export async function loadOmniroData(session:Session):Promise<OmniroLoadedData>{const projects=(await api.projectsPage("page=1&pageSize=100&sortBy=name&sortOrder=asc")).data,partial:string[]=[];const safe=async<T>(name:string,promise:Promise<T>,fallback:T)=>promise.catch(()=>{partial.push(name);return fallback});const[scanPage,assignments,designs]=await Promise.all([safe("room-scanner",hasPermission(session,"room_scans.read")?api.roomScans():Promise.resolve({data:[],pagination:{page:1,pageSize:25,total:0,totalPages:0}}),{data:[]as RoomScanDto[],pagination:{page:1,pageSize:25,total:0,totalPages:0}}),safe("technical-assignment",hasPermission(session,"technical_assignments.read")?api.technicalAssignments():Promise.resolve([]),[]as TechnicalAssignmentDto[]),safe("design-project",hasPermission(session,"design_projects.read")?api.designProjects():Promise.resolve([]),[]as DesignProjectDto[])]);const byProject=new Map<string,BuildingSources>();for(const project of projects)byProject.set(project.id,{project,scans:scanPage.data.filter(x=>x.projectId===project.id),assignments:assignments.filter(x=>x.projectId===project.id),designs:designs.filter(x=>x.projectId===project.id),sourceFailures:partial});return{projects,byProject,partial}}
